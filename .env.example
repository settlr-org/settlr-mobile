# Default (staging) — used by CI fallback and EAS preview channel
# For local, copy .env.development.example to .env.local; for production copy .env.production.example
EXPO_PUBLIC_API_URL=https://settlrapi.theswissknife.com
